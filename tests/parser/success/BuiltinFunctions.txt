##builtins

//вызывать встроенные функции можно обычным способом например:
${+(1,2)}

//либо операторной нотацией
//поддерживаются бинарные и префиксные унарные операторы
//приоритет и ассоциотивность операций имеют место

${1 + 2 * 3 == "7"}
$some_function(1 + 2, 1==2)

${-1 + 2}

${-(-(-1) * 5)}