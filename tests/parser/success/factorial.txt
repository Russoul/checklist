##CheckList Factorial

//TODO нужная операторная нотация для упрощения написания и чтения кода

$$factorial(n)
    $if{n == 0 || n == 1}
        1
    $else
        ${n * factorial(n - 1)}


<-Введите число

->n


$factorial($n)